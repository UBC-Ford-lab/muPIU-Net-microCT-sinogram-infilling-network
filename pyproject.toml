[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "mupiu-net"
version = "0.1.0"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["unet_pipeline*", "metric_calculators*", "reconstruction*"]
